name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        version=$(date +%s)
        docker build . --file Dockerfile --tag squid-proxy:$version
